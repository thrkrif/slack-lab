# 키 이름만 적는다. 실제 값은 .env에 넣고 커밋하지 않는다.
# Spring relaxed binding: SLACK_BOT_TOKEN -> slack.bot-token
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=

# ollama list 로 확인한 모델 ID를 쓴다 (추측 금지)
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=qwen2.5:7b
